int foo(a, b : int) {
    return (a + b) / 2;
}

int main() {
    a, b: int;
    a = 5;
    b = 2;
    if a > 2 && b < 4 || b == 3 then 
        write(foo(a,b));
    else 
        write("Condition not met");
    return 0;
}
